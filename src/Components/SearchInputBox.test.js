import { render } from "@testing-library/react";
import React from "react";
import SearchInpuBox from "./SearchInputBox";
describe("SearchInputBox", () => {
  it("hello", () => {
    render(<SearchInpuBox />);
  });
});
